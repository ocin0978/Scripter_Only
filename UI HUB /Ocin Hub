game:GetService("TextChatService").TextChannels.RBXGeneral:SendAsync("Loading...")
task.wait(3.5)
game:GetService("TextChatService").TextChannels.RBXGeneral:SendAsync("Access Granted!")
task.wait(3.5)
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()
local Window = WindUI:CreateWindow({
    Title = "Ocin Hub",
    Icon = "monitor",
    Author = "Access Granted!",
    Folder = "Ocin Hub Files",
    Size = UDim2.new(0.8, 0, 0.6, 0),
    Transparent = true,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    BackgroundImageTransparency = 0.42,
    HideSearchBar = true,
    ScrollBarEnabled = true,
})
local Tab = Window:Tab({
    Title = "Library Hub",
    Icon = "github",
    Locked = false,
})
local Button = Tab:Button({
    Title = "Tiger X V4",
    Desc = "",
    Locked = false,
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/BalintTheDevXBack/Universal/refs/heads/main/TigerXHubV4"))()
    end
})
local Tab = Window:Tab({
    Title = "Scripts",
    Icon = "scroll",
    Locked = false,
})
